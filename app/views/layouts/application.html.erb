<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "quitcoin" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= link_to "Sign up", new_user_registration_path unless current_user %>
    <%= link_to "Sign in", new_user_session_path, method: :get if !current_user %>
    <%= link_to "Sign out", destroy_user_session_path, method: :delete if current_user %>
    <%= link_to "About", "#" %>
    <%= link_to "Bank", "#" if current_user %>
    <%= link_to "Progress", "#" if current_user %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class = row column>
      <%= yield %>
    </div>
  </body>
</html>
